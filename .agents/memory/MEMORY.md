- [FULL_SENSOR trick doesn't always beat rotation lock](android-orientation-lock-vs-system-setting.md) — on some devices even all-4-orientations still obeys the OS lock; force one explicit orientation via a raw accelerometer listener instead.
- [SharedPreferences JSON list pattern](shared-prefs-json-list-pattern.md) — reusable pattern for a local "library" list (history, downloads, etc.): JSON-encoded list in one SharedPreferences key with loadAll/add/remove/clear.
- [Loopback server URLs are session-scoped](loopback-manifest-server-lifecycle.md) — never persist/reuse a 127.0.0.1 URL across screens/sessions; republish the raw content fresh every time.
- [ffmpeg-kit https flavor bloats APK](ffmpeg-apk-size-min-flavor.md) — download remote sources via Dart's `http` first, feed ffmpeg only local files, then the tiny `min` flavor suffices for `-c copy` remux.
- [Replacing ffmpeg with MP4Parser](ffmpeg-to-mp4parser-migration.md) — MP4Parser only remuxes (no decode); pair with native `MediaMetadataRetriever` for thumbnails, and add aggressive ProGuard keep rules (reflection-based box lookup).
- [Accelerometer axis-to-orientation mapping easy to flip 180°](accelerometer-rotation-mapping.md) — "rotates but upside-down" bug is usually a swapped landscapeLeft/Right sign, not the debounce logic.
- [Android edge-swipe-to-go-back needs a manifest flag](android-predictive-back-swipe.md) — if swipe-back fails app-wide (not just one screen), check for missing `android:enableOnBackInvokedCallback="true"` before adding custom gesture code.
- [Forcing compileSdk on Flutter plugin subprojects](gradle-plugin-compilesdk-override.md) — `withPlugin`+`extensions.configure` silently gets overwritten by the plugin's own later `android{}` block; must guard with `state.executed` + `afterEvaluate`.
- [XML with two top-level elements silently defeats try/catch parsing](xml-multi-root-silent-parse-failure.md) — a stray tag before the real root makes `XmlDocument.parse` throw; regex-strip it before parsing, don't rely on the parser to handle it.
